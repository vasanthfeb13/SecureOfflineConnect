{
  "name": "SecureOfflineConnect",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"Build script not defined\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
